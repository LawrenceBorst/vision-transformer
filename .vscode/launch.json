{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "main.py",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "args": ["--debug", "--epochs", "1", "--limit_data", "100"],
            "console": "integratedTerminal"
        }
    ]
}